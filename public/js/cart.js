class CartItem{
    
    constructor( productId, productName , price, counter)
    {
        this.productId   = productId;
        this.productName = productName;
        this.price       = price;
        this.counter     = counter;
    }

}